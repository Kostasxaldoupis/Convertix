# File Converter

A simple desktop and CLI file converter built with Python.

## Features
- PNG → JPG
- JPG → PNG
- Images → PDF
- PDF → DOCX
- CSV → JSON

## GUI
Run:

python gui.py

## CLI

python cli.py input_file output_file

Example:

python cli.py image.png image.jpg
python cli.py image.png image.pdf